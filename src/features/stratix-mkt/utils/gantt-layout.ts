// Ancho de la columna de un día en el Gantt. Lo necesitan el encabezado (para su ancho) y la
// barra (para calcular su desplazamiento en px), así que vive en un solo lugar y viaja al CSS
// como variable. Es una constante: sin lógica que testear, va como archivo suelto.
export const DIA_W = 44
