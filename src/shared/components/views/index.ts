export { default as CalendarView } from './CalendarView'
export type { CalendarItem } from './types'

// The public face of the shared views. Features import from here; the pieces a view is built
// from —cells, boxes, the grid maths— stay private to its folder.
