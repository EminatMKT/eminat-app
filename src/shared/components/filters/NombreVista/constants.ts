/** Confirma el diálogo desde el campo: en uno de un solo input, ir al botón es un paso de más. */
export const ENTER = 'Enter'

/** Angosto a propósito: adentro hay un campo y dos botones, y un modal de 30rem para eso deja el
 *  input flotando en un vacío que hace dudar de si falta algo por llenar. */
export const MODAL_REM = 24

/** Las dos claves i18n del diálogo: el rótulo del campo y la acción que lo confirma. */
export const FIELD_KEY = 'common.filter.saveName'
export const SAVE_KEY = 'common.filter.saveDo'
