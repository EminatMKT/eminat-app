export { default as AccessDenied } from './AccessDenied'

// Barrel of the access screens. Only re-exports: each component keeps its single default in its
// own folder, and this is the one place its public name is written.
